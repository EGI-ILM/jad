meta {
  name: Prepare Dataplane
  type: http
  seq: 5
}

post {
  url: {{hostname}}/api/mgmt/v5alpha/dataplanes/{{provider_id}}
  body: json
  auth: inherit
}

headers {
  Host: {{cpBaseUrl}}
}

body:json {
  {
    "allowedSourceTypes": [
      "HttpData",
      "HttpCertData"
    ],
    "allowedTransferTypes": [
      "HttpData-PULL"
    ],
    "url": "http://dataplane.edc-v.svc.cluster.local:8083/api/control/v1/dataflows"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
